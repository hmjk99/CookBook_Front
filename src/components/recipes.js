import { useState } from "react";

const Recipes = (props) => {
  return (
    <>
      <div>
        {recipes.map((recipe) => {
          <ul>
            <li></li>
          </ul>;
        })}
      </div>
    </>
  );
};

export default Recipes;
